/** Validation regex for zipCode */
export const zipCodePattern = /[a-zA-Z0-9]{1,20}/;

/** Validation regex for countryCode */
export const countryCodePattern = /[a-zA-Z0-9]{2}/;

/** Validation regex for cityName */
export const cityNamePattern = /[a-zA-Z -]{1,35}/;

/** Validation regex for stateCode */
export const stateCodePattern = /[a-zA-Z0-9]{1,2}/;

/** Validation regex for postalBox */
export const postalBoxPattern = /[a-zA-Z0-9]{1,10}/;

/** Validation regex for locationCode */
export const locationCodePattern = /[a-zA-Z]{3}/;

/** Validation regex for terminal */
export const terminalPattern = /[a-zA-Z0-9]{1,25}/;

/** Validation regex for currencyCode */
export const currencyCodePattern = /[A-Z]{3}/;

/** Validation regex for departureTerminal */
export const departureTerminalPattern = /[a-zA-Z]{1,25}/;

/** Validation regex for aircraftCode */
export const aircraftCodePattern = /[a-zA-Z0-9]{1,3}/;
